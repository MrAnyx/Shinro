{
    "ConnectionStrings": {
        "Database": "Host=localhost;Port=5432;Database=shinro;Username=shinro;Password=shinro"
    },
    "AllowedHosts": "*",
    "Logging": {
        "NLog": {
            "AutoShutdown": true,
            "RemoveLoggerFactoryFilter": true,
            "RegisterServiceProvider": true,
            "IncludeScopes": true,
            "ReplaceLoggerFactory": true
        }
    },
    "NLog": {
        "autoReload": false,
        "throwConfigExceptions": true,
        "internalLogLevel": "Warn",
        "internalLogFile": "${gdc:nlogFilePath}",
        "time": {
            "type": "FastLocal"
        },
        "targets": {
            "console": {
                "type": "Console",
                "layout": "[${longdate} ${uppercase:${level}} ${logger}] ${message}${onexception:${newline}${exception:format=tostring}}"
            }
        },
        "rules": [
            {
                "logger": "Microsoft.*",
                "finalMinLevel ": "Warn"
            },
            {
                "logger": "*",
                "minLevel": "Trace",
                "writeTo": "console"
            }
        ]
    }
}
